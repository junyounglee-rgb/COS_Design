# 번역 워크플로우 비교 v2: 현재 Lokalise → v2 추천안 (A 프록시 + D cron)

> 비교 대상: `translation_pipeline.md` (현재, 사고 5건 발생) vs `redesign_final.md` (v2 수렴안, Critic 52/60)
> 기준일: 2026-04-18 / Iteration v2 수렴
> 이전 비교: `workflow_comparison.md` (Lokalise vs Google Sheet, Google Sheet 편애 논란으로 폐기)

---

## 전체 요약

| 항목 | 현재 (Lokalise) | v2 개선 (A 프록시 + D cron) | 변화 |
|------|----------------|-------------------------|------|
| **총 작업 단계** | 17단계 | 6단계 (자동 4 + 사람 2) | **-11단계** |
| **PM 수작업** | 11단계 (46분/회) | 0단계 | **-100%** |
| **기획자 오타 수정 시간** | 배치 대기 + PM 46분 (수 시간~1일) | translations_ko.xlsx 직접 수정 + `tag=re` → 일상 PR (수 분) | **-98%** |
| **5건 사고 재발 차단** | 0건 차단 (5/5 재현) | **5/5 차단** (0초 또는 Phase 0 Exit 이후) | 구조적 해결 |
| **편법 경로 차단** | 0/8종 차단 (E1~E5, F1/F2/F4) | **8/8 차단** | 구조적 해결 |
| **단일 장애점** | PM 부재 = 전체 중단 | Lambda fail-close + SOP 부록 | 해소 |
| **VNG 역류 위험** | 상시 (cos-main 쓰기 권한 보유) | **Phase 0 Exit부터 0** (권한 회수) | 해소 |
| **Lokalise 웹 ko 직수정** | 가능 (04-17 사고 원인) | **403 거부 (0초 차단)** | 구조적 금지 |
| **`.pb` 직접 커밋** | 가능 (03-07 사고 원인) | **pre-receive reject** | 구조적 금지 |
| **태깅 누락** | 수동 의존 (03-11 사고 원인) | **`tag` 컬럼 자동 매핑** | 구조적 제거 |
| **개발 공수** | — | 1~2명 × 4~6주 | Phase 0~4 |
| **고정 제약 준수** | — | 6/6 (Lokalise 유지 포함) | 조직 충돌 없음 |

---

## 현재 워크플로우 (사고 5건 재현 가능)

```
┌────────────────────────────────────────────────────────┐
│ 기획자                                                  │
│ ─────────────────────────────────────                  │
│ 1. translations.xlsx에 한글 입력                        │
│    ⚠ modes.xlsx `name`에도 ko 입력 가능 (사고 02-05)     │
└────────────────────────┬───────────────────────────────┘
                         ↓
              ⏳ PM 배치 처리 대기 (수 시간~1일)
                         ↓
┌────────────────────────────────────────────────────────┐
│ PM — 46분 수동 작업                                     │
│ ─────────────────────────────────────                  │
│ 2.  Fork → 번역키 추출 (cos-common) + 20초 대기         │
│ 3.  exports/ 오탈자·누락 검수                           │
│ 4.  7종 카테고리 수동 분류 (dialog/cookie/quests/...)   │
│ 5.  Lokalise 접속 + Upload                              │
│     ⚠ "Differentiate keys by file" 해제 실수 가능       │
│        → 대표키 병합 사고 (02-20)                        │
│ 6.  Skipped 키 처리 → 재업로드                          │
│ 7.  Tag 수동 입력 — 누락 가능 (사고 03-11)              │
│ 8.  기획팀·PM팀 Slack 공지                              │
└────────────────────────┬───────────────────────────────┘
                         ↓
┌────────────────────────────────────────────────────────┐
│ 번역팀                                                   │
│ ─────────────────────────────────────                   │
│ 9.  Lokalise 로그인 + 번역 입력 (en/ja/zh/th/id)        │
│     ⚠ ko 필드도 수정 가능 (사고 04-17 rev 누적)         │
│     ⚠ 개인 API 토큰 발급 가능 (편법 F2 상시 열림)       │
└────────────────────────┬───────────────────────────────┘
                         ↓
┌────────────────────────────────────────────────────────┐
│ VNG 법인 (베트남)                                        │
│ ─────────────────────────────────────                   │
│ 10. Lokalise cos-main 직접 접속 → id/vi 편집            │
│     ⚠ ko·en 필드도 편집 가능 (사고 02-20, E5 역류)      │
│     ⚠ 권한이 과대 (cos-main 쓰기 상시)                  │
└────────────────────────┬───────────────────────────────┘
                         ↓
┌────────────────────────────────────────────────────────┐
│ PM — Sync 수동                                          │
│ ─────────────────────────────────────                   │
│ 11. Sync 페이지 → "Just Do It" 클릭                     │
│ 12. 10분 대기                                           │
│ 13. Fork 자동 push 확인                                 │
│ 14. 완료 공지                                           │
└────────────────────────┬───────────────────────────────┘
                         ↓
               [게임 반영 완료]
                         ↓
        ❓ 오타 발견?
          ├─ YES → 기획자 → 로컬 datasheet 실행 후
          │       ⚠ .pb만 커밋 가능 (사고 03-07 잔여물)
          │       → 또는 ⏳ 다음 배치 대기 루프
          └─ NO → 종료
```

### 사고 발생 포인트 (5건)

| 시점 | 사고 | 발생 원인 (현재 구조 상시 열림) |
|------|------|----------------------------|
| 2026-02-05 | Modes^1303 이중 소스 | 기획자가 modes.xlsx `name` + translations.xlsx ko **양쪽에 입력** → datasheet가 서로 다른 값 생성 |
| 2026-02-20 | VNG 대표키 병합 | VNG가 Lokalise `cos-main`에서 인니/베트남어 업로드 시 **"Differentiate keys by file"** 체크 실수 + VNG가 ko·en도 편집 가능 |
| 2026-03-07 | skill_description 잔여물 | 기획자가 로컬 `datasheet` 실행 → **.pb만 수정 + 원본 excel 커밋 누락** → 다른 사람이 재생성 시 잔여 |
| 2026-03-11 | 303건 태깅 누락 | PM이 애플스토어 검수용 업로드 시 **수동 태깅 단계 누락** → Lokalise 번역팀이 대상 키 파악 불가 |
| 2026-04-17 | rev 누적 복합 | 지희가 Lokalise 웹에서 ko `못했어~` 직수정 + Excel 미갱신 → Sync 시 **rev 누적 → 기획 의도와 다른 ko 배포** |

---

## v2 개선 워크플로우 (A 프록시 + D cron)

```
┌────────────────────────────────────────────────────────┐
│ 기획자                                                  │
│ ─────────────────────────────────────                  │
│ 1. translations.xlsx에 str_id + ko + tag 입력           │
│    🛡 Validator (a): modes.xlsx 등에 ko 입력 → CI reject │
│    🛡 Validator (b): translations.xlsx ko 중복 → reject  │
└────────────────────────┬───────────────────────────────┘
                         ↓
        ┌────────────────────────────────────┐
        │ CI 자동 (PR 머지 시 + 매 빌드)      │
        │ ───────────────────────────────    │
        │ • Validator 3종 검사                │
        │ • datasheet rev-dedup               │
        │ • pb 자동 생성 (레포에 커밋 안 함)    │
        │ • pre-receive hook: pb push 거부    │
        │ • tag→Lokalise publish 1:1 매핑     │
        └────────────────┬────────────────────┘
                         ↓
              [클라이언트/서버 배포 반영]
                         ↓
        ┌────────────────────────────────────┐
        │ 번역팀                               │
        │ ───────────────────────────────    │
        │ 2. Lokalise 로그인 + 번역 입력       │
        │    (en/ja/zh/th/id 5개 언어만)       │
        │ 🛡 ko 필드는 읽기 전용 (A 프록시 403) │
        │ 🛡 개인 API 토큰 발급 불가 (역할 제한) │
        └────────────────┬────────────────────┘
                         ↓
        ┌────────────────────────────────────┐
        │ VNG 법인 (베트남)                    │
        │ ───────────────────────────────    │
        │ 3. cos-vng 프로젝트에서 id/vi 편집   │
        │ 🛡 cos-main 쓰기 권한 0 (Phase 0 Exit)│
        │ 🛡 cos-main으로의 역류 경로 소멸     │
        └────────────────┬────────────────────┘
                         ↓
        ┌────────────────────────────────────┐
        │ CI 자동 (10분 cron)                 │
        │ ───────────────────────────────    │
        │ • xlsx ko 해시 비교                 │
        │ • Lokalise에 표류 발생 시            │
        │   delete_rev + create_translation   │
        │ • 프록시 우회 공격 이중 방어선        │
        └────────────────┬────────────────────┘
                         ↓
              [게임 반영 완료]
                         ↓
        ❓ 오타 발견?
          └─ 발견자가 translations_ko.xlsx에서
             ko 수정 + tag=re
                         ↓
        ┌────────────────────────────────────┐
        │ 4. git commit → PR → 리뷰 → main 머지 │
        │    (1~3번과 동일한 일상 PR 경로)      │
        │    CI 자동: tag=re 감지 →            │
        │    Lokalise ko 덮어쓰기 + 번역팀 알림 │
        └────────────────────────────────────┘
```

### 🛡 구조적 차단 장치 (편법 8종 전부 막힘)

| 편법 | 차단 장치 | 원복 지연 |
|------|---------|----------|
| E1 Lokalise 웹 ko 직수정 | **A 프록시 403** + D cron 10분 | 0초 (차단) |
| E2 대표키 병합 실수 | CI `differentiate_by_file=true` 하드코딩 + Validator (b) | 0초 (차단) |
| E3 `.pb` 직접 편집 커밋 | `.gitignore` + **서버 pre-receive reject** | 0초 (차단) |
| E4 git 커밋 누락 + 로컬 datasheet | **CI only pb 생성** (로컬 pb 레포 진입 불가) | 0초 (차단) |
| E5 VNG cos-main 역류 | **Phase 0 Exit부터 VNG cos-main 쓰기 0** | 0초 (차단, Phase 0 이후) |
| F1 xlsx 이중 소스 (modes/dialog_groups에 ko) | **Validator (a) CI reject** | 0초 (차단) |
| F2 개인 API 토큰 | 역할별 **토큰 발급 금지** + **IP allowlist** | 0초 (차단) |
| F4 pre-receive bypass | **GitHub bypass OFF** + 서버 hook (--no-verify 영향 없음) | 0초 (차단) |

---

## 단계별 변화 (Before → After)

```
기획자:
  현재 ████████                    1단계 (xlsx 입력)
  개선 ████████                    1단계 (xlsx 입력 + tag)
       → 동일, 다만 Validator 3종이 실수 즉시 차단

PM:
  현재 ████████████████████████   11단계 (수작업 46분)
  개선                             0단계
       → 완전 제거 (CI 자동화 + 일상 PR 경로 통합)

번역팀:
  현재 ████████                    3단계 (Lokalise 접속/필터/입력)
                                   ⚠ ko 필드도 수정 가능
  개선 ████████                    3단계 (동일, 단 ko 필드 read-only)
       → UI 동일, 권한만 축소 (학습 비용 0)

VNG 법인:
  현재 ████████                    1단계 (cos-main 직접 편집)
                                   ⚠ 역류 상시 위험
  개선 ████████                    1단계 (cos-vng 전용)
       → 프로젝트 분리, 역류 경로 소멸

자동화 (CI/프록시/cron):
  현재 ░░░░░░░░                    0단계 (모두 수동)
  개선 ████████████████████████    4단계 (Validator / 프록시 / 태깅 / cron)
       → 전면 도입

총합:
  현재 ██████████████████████████████████████ 17단계 (사람)
  개선 ████████████                           6단계 (사람 2 + CI 4)
                                              -11단계 (-65%)
```

---

## 사라지는 사고 (5건) × 구조적 차단 근거

| 사고 | 현재 구조에서의 재현 시나리오 | v2 차단 장치 (Phase N Exit부터 유효) | 주차 |
|------|--------------------------|-------------------------------|------|
| **02-05** 이중 소스 | 기획자가 `modes.xlsx name`에 ko 주입 → datasheet가 translations.xlsx ko와 충돌 | Phase 2 **Validator (a)** datasheet 변환 거부 | Week 2~3 |
| **02-20** VNG 병합 | VNG가 cos-main 인니어 업로드 시 "Differentiate keys by file" 해제 실수 | **Phase 0** VNG 쓰기 권한 0 + 대리 입력 창구에서 CI 자동 `differentiate_by_file=true` | Week 0 |
| **03-07** pb 잔여 | 기획자가 로컬 datasheet 실행 → .pb만 커밋 → 원본 excel 미커밋 | Phase 2 `.gitignore` + **서버 pre-receive hook** reject | Week 2~3 |
| **03-11** 태깅 누락 | PM이 303건 업로드하며 **수동 태그 단계 누락** | Phase 3 `tag` 컬럼 스키마 강제 + CI가 tag→publish 1:1 매핑 | Week 3~4 + D+3 |
| **04-17** rev 누적 | 지희가 Lokalise 웹 ko 직수정 → Sync 시 rev 누적 | Phase 1 **A 프록시 403** + Phase 3 **D cron 10분 복구** | Week 1~2 |

**전체 차단률**: 5/5 (탐지 0, 통과 0)

---

## 역할 변화

| 역할 | 현재 | v2 개선 | 변화 요약 |
|------|------|--------|---------|
| **기획자** | translations.xlsx 한글 입력 + 오타 시 PM 호출 | translations_ko.xlsx + `tag` 입력 / 오타 시 직접 수정 + `tag=re` (일상 PR 경로) | 본인 수정 가능, PM 대기 소멸, 별도 커맨드 없음 |
| **PM** | 번역 파이프라인 전담 (46분/회, 11단계) | **관여 없음** (CI·프록시·cron 자동) | 역할 제거 |
| **번역팀** | Lokalise에서 6개 언어 편집 (ko 포함) | Lokalise에서 **5개 언어만** (ko read-only) | UI·단계 동일, 권한만 축소 |
| **VNG 법인** | cos-main 직접 편집 (ko·en·id·vi 전체) | **cos-vng 전용**, id/vi만 편집 (Phase 4 완성 후) | 프로젝트 분리 |
| **CI** | 번역키 추출 버튼만 (Fork) | Validator 3종 / 프록시 / 태깅 / 10분 cron / pb 빌드 / pre-receive hook | 전면 자동화 |
| **프록시 Lambda (신규)** | — | Lokalise API 게이트웨이 + fail-close SOP owner | 신규 운영 |

---

## 속도 비교

```
현재 — 기획자 오타 수정 시:
  기획자 수정 → ⏳ PM 배치 대기 → PM 46분 작업 → Sync 10분 → 반영
              ───────────────────────────────────────────
              │ 총 소요: 수 시간 ~ 1일 (배치 주기 의존)    │
              │ + 수정 반복 시 배치 × N배 누적 지연         │
              ───────────────────────────────────────────

v2 — 기획자 오타 수정 시:
  발견자가 translations_ko.xlsx ko 수정 + tag=re → PR → 머지 → CI 자동
  ─────────────────────────────────────────────────────────────────
  │ 총 소요: 수 분 (일상 PR 경로 동일)                            │
  │ 신규 원문 등록과 완전히 같은 흐름 (커맨드·예외 경로 없음)      │
  │ CI가 tag=re 감지 → Lokalise ko 자동 덮어쓰기 + 번역팀 알림      │
  │ PM 개입 0분 / 배치 대기 0 / 담당자 소환 0                      │
  ─────────────────────────────────────────────────────────────────

v2 — 프록시 우회 공격 발생 시 (만에 하나):
  공격 시도 → A 프록시 403 (0초)
           │
           └─ 우회 성공 시 → D cron 10분 내 자동 복구
              ─────────────────────────────────────
              │ 0초: 프록시가 첫 방어선              │
              │ ≤10분: cron이 이중 방어선 (탐지+복구) │
              ─────────────────────────────────────
```

---

## Phase 전환 타임라인 (v2 Phase 0~5)

```
W0 ─ Phase 0 ─ VNG 권한 회수 + 측정
               🛡 E5/02-20 차단 시작

W1~2 ─ Phase 1 ─ 프록시 + IP allowlist + 역할 강등
                 🛡 E1/04-17 + F2 차단 시작

W2~3 ─ Phase 2 ─ Validator 3종 + pb gitignore + pre-receive hook
                 🛡 F1/02-05 + E3/03-07 + E4 + F4 차단
                 🛡 M2 일상 PR 경로로 수렴 (오타 수정 = 신규 등록과 동일)

W3~4 + D+3 ─ Phase 3 ─ tag 컬럼 + 태깅 자동화 + D cron
                        🛡 03-11 차단 + 프록시 우회 복구 계층

W4~5 ─ Phase 4 ─ cos-vng 프로젝트 완성 + VNG 단방향 반영
                 🛡 E5 장기 구조 완결

W5~6+ ─ Phase 5 ─ (선택) 고정 제약 재검토 / 장기 실패 모드 PoC
```

각 Phase마다 롤백 포인트 명시 — 문제 발생 시 직전 상태로 복구 가능.

---

## 연간 효과 추정 (참고)

| 항목 | 현재 | v2 | 연간 절감 |
|------|------|-----|---------|
| PM 수동 업로드 시간 | 46분 × 주 2~4회 × 52주 = **160~320시간** | 0시간 | **160~320시간** |
| 기획자 오타 수정 대기 | 배치 주기 × 수정 횟수 | 30초/건 | **측정 불능 규모** |
| 사고 대응 공수 | 사고당 2~8시간 × 5건/2.5개월 → 연환산 48~192시간 | **구조적 차단** | **48~192시간** |
| 번역팀 재작업 (rev 누적 원인) | 사고당 수 시간 | **0** | 대폭 감소 |

> 2.5개월 5건 사고 = 연환산 **24건/년**. v2가 구조적으로 막아줌 → 기대 사고 재발률 0.
> 프록시 우회 공격이 실제 발생해도 D cron 10분 내 복구.

---

## 변경에 드는 비용 (조직 관점)

| 항목 | 비용 |
|------|------|
| 개발 공수 | 1~2명 × 4~6주 (Phase 0~4) |
| Lokalise 구독 | **유지** (고정 제약) |
| 번역팀 UI 학습 | 0 (Lokalise 동일, ko만 read-only) |
| 기획팀 Excel 친숙도 | 0 (translations.xlsx 동일) |
| VNG 법인 | Phase 0~3 과도기 **2~4주** Slack 대리 입력 창구 운영 (기획팀 1~2명 당번) |
| 프록시 Lambda 운영 | dev팀 or 인프라팀 중 owner 지정 (fail-close SOP 부록 1페이지) |

---

## 관련 문서

| 문서 | 역할 |
|------|------|
| [redesign_final.md](./redesign_final.md) | **최종 추천안 상세** — 후보 6개 비교, 편법 8종×6후보 매트릭스, Phase 0~5 Entry/Exit/롤백 |
| [README.md](./README.md) | 프로젝트 진입점 (하네스 기록 + v1/v2 비교) |
| [case_study_2026-04-17_rev_pollution.md](./case_study_2026-04-17_rev_pollution.md) | 5건 사고 원인 분석 (입력 근거) |
| [translation_pipeline.md](./translation_pipeline.md) | 현재 파이프라인 기술 문서 (as-is) |
| [harness_artifacts/iter-2/review-v2.md](./harness_artifacts/iter-2/review-v2.md) | Critic 독립 채점 52/60 근거 |
| [harness_artifacts/iter-2/handoff-v2.md](./harness_artifacts/iter-2/handoff-v2.md) | 수렴 판정 + Phase 0 착수 준비 + v3 숙제 |

---

## 한 줄 요약

> **현재**: 기획자 오타 1건 수정에 PM 46분 + 배치 대기, 사고 5건/2.5개월 재발.
> **v2**: 기획자가 translations_ko.xlsx에 ko 수정 + `tag=re` + 일상 PR (수 분), 사고 재발 0건 구조 달성 (5/5 차단, 편법 8/8 차단).
> **비용**: 개발 1~2명 × 4~6주, Lokalise 구독 유지, 번역팀 학습 비용 0.
