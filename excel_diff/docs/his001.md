# his001 — excel_diff 개발 히스토리

## 개요

| 항목 | 내용 |
|------|------|
| 문서번호 | his001 |
| 프로젝트 | excel_diff |
| 작성일 | 2026-04-23 |
| 작성자 | 이준영 (기획), Claude Code (구현) |

---

## 개발 배경

| 구분 | 내용 |
|------|------|
| 기존 | `cherry_pick.py` 내부에 diff 로직이 포함되어 있었음 |
| 목적 | 커밋 전 로컬 파일 vs 서버 파일 빠른 비교 (별도 툴로 분리) |
| 결정 | `diff_core.py` 독립 추출 + Streamlit UI + Windows bat 패키징 |

---

## 주요 개발 단계

| 단계 | 내용 | 결과 |
|------|------|------|
| 1 | `diff_core.py` 추출 | `cherry_pick.py`에서 diff 함수 독립, 외부 의존성 제거 |
| 2 | `app.py` 초안 (STEP 1) | 리포 경로 입력 + 로컬 파일 경로 직접 입력 방식 |
| 3 | developer 에이전트 코드 검토 (STEP 2) | `_ERR_REMOTE`/`_ERR_LOCAL` 센티널 도입, `local_mtime` 캐시 무효화 추가 |
| 4 | QA 검토 (STEP 3) | P2: `_local_mtime` 캐시 키 제외 버그 발견 |
| 5 | QA 피드백 반영 (STEP 4) | `local_mtime` 언더스코어 제거, `rel_path` 확장자 검사, `install.bat` `--python .venv` 명시 |
| 6 | bat 인코딩 버그 수정 | UTF-8 한국어 포함 bat → cmd.exe 파싱 실패 → 영어 전용으로 재작성 |
| 7 | 파일 검색 UI 추가 | 직접 경로 입력 → 검색창 + radio 리스트 방식으로 변경 |
| 8 | 리포 경로 단순화 | cos-data 고정 → 설정 패널로 이동, 브랜치만 메인 노출 |
| 9 | 커밋 히스토리 기능 추가 | `git log --follow`로 파일별 커밋 조회, 20개씩 페이징 |
| 10 | 버전간 비교 모드 추가 (v1) | 두 selectbox로 버전 선택 |
| 11 | 버전간 비교 모드 UI 개선 (v2) | 리스트 각 행에 ①/② 버튼 인라인 배치 |

---

## 주요 발견 사항

### @st.cache_data와 언더스코어 파라미터

- Streamlit `@st.cache_data`는 `_`로 시작하는 파라미터를 캐시 키에서 제외
- `_local_mtime` 파라미터 → 파일 변경 시 캐시 무효화 안 됨
- 수정: `local_mtime`으로 이름 변경 (언더스코어 제거)

### Windows bat 파일 인코딩

- Python Write 도구는 UTF-8로 파일 저장
- Windows `cmd.exe`는 bat 파일을 ANSI(CP949)로 파싱 → 한국어 포함 시 파싱 실패
- `chcp 65001`은 출력 코드페이지만 변경, bat 파일 파싱에는 영향 없음
- 수정: bat 파일 내 한국어 전부 영어로 교체

### install.bat .venv 미생성 문제

- PowerShell의 `cmd /c` 실행 시 bat 파일 인코딩 에러로 venv 생성 단계 실패
- 실제 사용 시 직접 더블클릭으로 실행 필요
- bat 영어화 후 정상 동작 확인

---

## 환경 이슈 및 해결

| 이슈 | 원인 | 해결 |
|------|------|------|
| cmd 창 뜨자마자 꺼짐 | bat 파일 UTF-8 한국어 → `cmd.exe` 파싱 실패 | `run.bat`/`install.bat` 영어 전용으로 재작성 |
| `_local_mtime` 캐시 무효화 안됨 | Streamlit `_` 접두사 파라미터 캐시 키 제외 규칙 | 파라미터명 `local_mtime`으로 변경 |
| `install.bat` `.venv` 미생성 | PowerShell 통한 bat 실행 시 인코딩 에러 | 영어화 후 해결, 직접 더블클릭 권장 |
| git log 한국어 깨짐 | PowerShell 콘솔 CP949 인코딩 | Streamlit 브라우저에서는 정상 (UTF-8 렌더링) |

---

## 파일 위치

| 파일 | 경로 |
|------|------|
| 메인 앱 | `D:\claude_make\excel_diff\app.py` |
| diff 코어 | `D:\claude_make\excel_diff\diff_core.py` |
| 설정 | `D:\claude_make\excel_diff\config.yaml` |
| 의존성 | `D:\claude_make\excel_diff\requirements.txt` |
| 설치 스크립트 | `D:\claude_make\excel_diff\install.bat` |
| 실행 스크립트 | `D:\claude_make\excel_diff\run.bat` |
| 스펙 문서 | `D:\claude_make\excel_diff\docs\spec001.md` |
| 히스토리 문서 | `D:\claude_make\excel_diff\docs\his001.md` |

---

## 미구현 항목

| 항목 | 비고 |
|------|------|
| 다중 파일 동시 비교 | 단일 파일 선택만 지원 |
| diff 결과 내보내기 (HTML/CSV) | 화면 렌더링만 지원 |
| 로컬 브랜치 비교 | origin 원격 브랜치만 지원 |
| 변경 행 수 요약 통계 | 시각화 없음 |
